import { FunctionComponent } from "react";
import styles from "./CancelButton.module.css";
const CancelButton: FunctionComponent = () => {
  return <div className={styles.buttonText}>Cancel</div>;
};

export default CancelButton;
