import React from 'react'
import './SelectionAdderButton.css'
export default function SelectionAdderButton (props) {
	return (
		<div className={`SelectionAdderButton_SelectionAdderButton ${props.className}`}>
		</div>
	)
}