import React from 'react'
import './Property1SomeConflicts.css'
export default function Property1SomeConflicts (props) {
	return (
		<div className={`Property1SomeConflicts_Property1SomeConflicts ${props.className}`}>
			<div className='Available'/>
		</div>
	)
}